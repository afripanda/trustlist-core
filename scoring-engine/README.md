# scoring-engine

The scoring-engine kernel: event subscription, score write-back and
version stamping. The composite formula, category weights and feature
pipelines are Stage 2 work and are not part of the kernel.

**Status:** placeholder. The kernel skeleton is scaffolded in Stage 0; the full
scoring engine is built across the Stage 2 implementation issues.

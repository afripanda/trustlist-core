# event-schema

JSON Schema definitions for event-bus payloads, keyed by `event_type` and
registered with the RedPanda schema registry. Schemas are CI-validated against
the registry on every pull request.

**Status:** placeholder. Populated from Stage 0 issue 11 (event-bus deployment)
onward, and extended by each collector and consumer as topics are added.

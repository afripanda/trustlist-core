# event-bus-sdk

Producer and consumer SDKs for the RedPanda event bus, in Python and
TypeScript. The SDKs implement typed event construction, schema-registry
integration, idempotency-key derivation, distributed-tracing context
propagation and back-pressure handling.

**Status:** placeholder. Implemented in Stage 0 issue 13 (Python) and issue 14
(TypeScript).
